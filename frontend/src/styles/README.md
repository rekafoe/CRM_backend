# 🎨 Структура стилей CRM

## 📁 Организация файлов

```
frontend/src/styles/
├── base/                    # Базовые стили
│   ├── buttons.css         # Стили кнопок
│   ├── modals.css          # Стили модальных окон
│   ├── forms.css           # Стили форм
│   ├── details.css         # Стили деталей заказов
│   ├── prepayment.css      # Стили предоплаты
│   ├── order-lists.css     # Стили списков заказов
│   └── index.css           # Импорт всех базовых стилей
├── components/             # Стили компонентов
├── pages/                  # Стили страниц
├── themes/                 # Темы и переменные
└── utilities/              # Утилитарные классы
```

## 🎯 Принципы организации

### 1. **Базовые стили (`base/`)**
- **Кнопки** - все варианты кнопок (.btn-primary, .btn-secondary, etc.)
- **Модалки** - базовые стили модальных окон
- **Формы** - стили полей ввода, селектов, чекбоксов
- **Детали** - стили для деталей заказов (.detail, .order-details)
- **Предоплата** - стили для предоплаты (.prepayment, .order-total__line)
- **Списки заказов** - стили для списков заказов (.order-list, .order-item, .virtualized-order)
- **Импортируются** в `app.css` через `@import './styles/base/index.css'`

### 2. **Стили компонентов**
- **Расположение**: `frontend/src/components/[ComponentName]/styles/`
- **Структура**:
  ```
  components/
  ├── OptimizedApp/
  │   ├── OptimizedApp.tsx
  │   └── styles/
  │       ├── OptimizedApp.css
  │       └── index.css
  ```

### 3. **Импорт стилей**
```tsx
// В компоненте
import "./styles/index.css";

// В app.css
@import './styles/base/index.css';
```

## ✅ Преимущества новой структуры

### **1. Устранение дублирования**
- ❌ **Было**: `.btn-primary` в 15+ файлах
- ✅ **Стало**: `.btn-primary` только в `base/buttons.css`

### **2. Консистентность**
- Единые стили для всех кнопок
- Единые стили для всех модалок
- Предсказуемое поведение

### **3. Легкость поддержки**
- Изменения в одном месте
- Четкая структура файлов
- Простота навигации

### **4. Модульность**
- Каждый компонент имеет свои стили
- Базовые стили переиспользуются
- Легко добавлять новые компоненты

## 🚀 Как использовать

### **Создание нового компонента:**
1. Создать папку `components/NewComponent/`
2. Создать `NewComponent.tsx`
3. Создать папку `styles/`
4. Создать `NewComponent.css` и `index.css`
5. Импортировать `import "./styles/index.css"`

### **Добавление новых базовых стилей:**
1. Создать файл в `styles/base/`
2. Добавить импорт в `styles/base/index.css`
3. Стили автоматически доступны везде

## 📋 Чек-лист миграции

- [x] Создать структуру папок
- [x] Консолидировать стили кнопок
- [x] Консолидировать стили модалок
- [x] Создать базовые стили форм
- [x] Переместить OptimizedApp.css
- [ ] Удалить дублирующиеся стили из других файлов
- [ ] Протестировать приложение
- [ ] Обновить документацию

## 🔧 Команды для работы

```bash
# Создание структуры для нового компонента
mkdir -p components/NewComponent/styles

# Перемещение существующих стилей
mv components/Component/Component.css components/Component/styles/

# Обновление импортов
# Заменить: import "./Component.css"
# На: import "./styles/index.css"
```
