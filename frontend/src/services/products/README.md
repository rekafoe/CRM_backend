# Product Service - Модульная структура

## 📁 Структура

```
products/
├── index.ts              # Главный экспорт (обратная совместимость)
├── types.ts              # Все типы и интерфейсы
├── categories.ts         # Работа с категориями
├── products.ts           # Работа с продуктами
├── materials.ts          # Работа с материалами продуктов
├── operations.ts         # Работа с операциями
├── services.ts           # Работа с услугами продуктов
├── configs.ts            # Работа с конфигурациями и параметрами
└── utils/
    ├── apiHelpers.ts     # Утилиты для API запросов
    └── cache.ts          # Утилиты для кэширования
```

## 🎯 Преимущества новой структуры

### 1. **Модульность**
- Каждый модуль отвечает за свою область
- Легко найти нужную функцию
- Проще тестировать отдельные модули

### 2. **Единообразная обработка ошибок**
- Все функции используют `apiRequest` и `apiRequestSafe`
- Типизированные ошибки через `ApiError`
- Консистентное логирование

### 3. **Улучшенное кэширование**
- Классы `SimpleCache` и `KeyedCache` для всех модулей
- Автоматическая инвалидация при изменениях
- Настраиваемое время жизни кэша

### 4. **Лучшая типизация**
- Все типы в одном месте (`types.ts`)
- Меньше `any`, больше строгих типов
- Улучшенная поддержка IDE

## 📖 Использование

### Импорт
```typescript
// Все функции доступны через единый индекс
import { 
  getProductCategories, 
  createProduct,
  getProductDetails,
  updateProduct,
  // ... и другие функции
} from '../../services/products';
```

### Примеры

```typescript
// Категории
import { getProductCategories, createProductCategory } from './products';
const categories = await getProductCategories();

// Продукты
import { getProductsByCategory, createProduct } from './products';
const products = await getProductsByCategory(1);

// Материалы
import { getProductMaterials, bulkAddProductMaterials } from './products';
const materials = await getProductMaterials(123);
await bulkAddProductMaterials(123, [{ material_id: 1, qty_per_sheet: 2 }]);

// Операции
import { getAllOperations, bulkAddProductOperations } from './products';
const operations = await getAllOperations();
```

## 🔧 Утилиты

### API Helpers
```typescript
import { apiRequest, apiRequestSafe, extractData } from './utils/apiHelpers';

// Безопасный запрос (возвращает fallback при ошибке)
const data = await apiRequestSafe(() => api.get('/endpoint'), 'контекст', []);

// Запрос с выбрасыванием ошибки
const data = await apiRequest(() => api.get('/endpoint'), 'контекст', null);
```

### Кэширование
```typescript
import { SimpleCache, KeyedCache } from './utils/cache';

const cache = new SimpleCache<string[]>(5 * 60 * 1000); // 5 минут
cache.set(['data']);
const data = cache.get(); // или null если истёк
```

## ✅ Миграция завершена

Старый файл `productService.ts` удален. Все импорты обновлены на новую структуру:

1. ✅ Все импорты обновлены на `services/products`
2. ✅ Используются новые утилиты для обработки ошибок
3. ✅ Все типы импортируются из `products/types`

## 📝 TODO

- [ ] Перенести fallback данные в отдельный файл
- [ ] Добавить unit-тесты для каждого модуля
- [ ] Создать документацию по каждому модулю
- [ ] Добавить примеры использования

