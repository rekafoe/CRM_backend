# CRM System Development Rules

## 🏗️ Architecture & Code Quality
- Всегда использовать Flux архитектуру для новых компонентов
- Приоритет: функциональность над дизайном
- Использовать TypeScript строго
- Следовать существующим паттернам кода

## 🚫 Anti-Patterns
- **НЕ ИСПОЛЬЗОВАТЬ ХАРДКОД** - все данные должны загружаться динамически из API/базы данных
- **НЕ ДЕЛАТЬ СЛИШКОМ БОЛЬШИЕ ФАЙЛЫ** - разбивать на компоненты и переиспользовать их
- **НЕ СОЗДАВАТЬ ДУБЛИРУЮЩИЕ ФАЙЛЫ** - всегда улучшать существующие компоненты вместо создания новых
- **НЕ ИСПОЛЬЗОВАТЬ ИНЛАЙН СТИЛИ** - всегда создавать отдельные CSS файлы для стилей
- Переиспользовать существующие компоненты, стили, кнопки в рамках проекта

## 📁 File Organization
- **ПРИОРИТЕТ: УЛУЧШЕНИЕ СУЩЕСТВУЮЩИХ ФАЙЛОВ** - не создавать новые, если можно улучшить старые
- Разбивать большие компоненты на мелкие переиспользуемые части
- Создавать общие компоненты для кнопок, форм, модальных окон
- **ВСЕГДА ВЫНОСИТЬ СТИЛИ В ОТДЕЛЬНЫЕ CSS ФАЙЛЫ** - никаких инлайн стилей!
- Использовать существующие стили и компоненты из проекта

## 🔧 Development Process
- Тестировать API endpoints перед интеграцией
- Документировать изменения в миграциях
- Проверять линтер перед коммитом
- Использовать существующие хуки и сервисы

## 🎨 UI/UX Guidelines
- Следовать существующему дизайну проекта
- Использовать уже созданные CSS классы и стили
- Применять консистентную цветовую схему
- Обеспечивать мобильную адаптивность

## 🎨 New Style System (для нового кода)
- **ИСПОЛЬЗОВАТЬ НОВУЮ СИСТЕМУ СТИЛЕЙ** для всех новых компонентов
- Импортировать компоненты из `../common` (Alert, Button, Modal, etc.)
- Использовать утилитарные CSS классы вместо инлайн стилей
- Применять CSS переменные для цветов (var(--text-primary), var(--bg-secondary))
- Использовать готовые компоненты: Alert, PriceIndicator, StatusBadge, Modal, Button, FormField
- Следовать паттерну: `className="flex items-center gap-2 text-primary"`

## 📊 Data Management
- Все данные загружать через API
- Использовать кэширование для оптимизации
- Валидировать данные на фронтенде и бэкенде
- Обрабатывать ошибки gracefully

## 🔄 Component Reusability
- Создавать переиспользуемые компоненты
- Использовать props для кастомизации
- Выносить общую логику в хуки
- Применять композицию вместо наследования
